(Age E 32)
(Age A 23)
(Age C 27)
(Age B 34)
(Age D 27)

(exec 0
    (, (Age $x $y))
    (O
        (pure
            (Lt_u32 $x $r)
            $r
            (lt_u32 (u32_from_string $y) (u32_from_string 28))
        )
    )
)

(exec 1
    (, (Age $x $y))
    (O
        (pure
            (Gt_u32 $x $r)
            $r
            (gt_u32 (u32_from_string $y) (u32_from_string 28))
        )
    )
)

(exec 2
    (, (Age $x $y))
    (O
        (pure
            (Eq_u32 $x $r)
            $r
            (eq_u32 (u32_from_string $y) (u32_from_string 27))
        )
    )
)

(exec 3
    (, (Age $x $y))
    (O
        (pure
            (Ne_u32 $x $r)
            $r
            (ne_u32 (u32_from_string $y) (u32_from_string 27))
        )
    )
)

(exec 4
    (, (Age $x $y))
    (O
        (pure
            (Le_u32 $x $r)
            $r
            (le_u32 (u32_from_string $y) (u32_from_string 28))
        )
    )
)

(exec 5
    (, (Age $x $y))
    (O
        (pure
            (Ge_u32 $x $r)
            $r
            (ge_u32 (u32_from_string $y) (u32_from_string 28))
        )
    )
)
