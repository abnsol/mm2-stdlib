(Age Biniam 32)
(Age Abenezer 23)
(Age Nahom 27)
(Age Nama 34)
(Age Eyasu 27)

(exec 0
    (, (Age $x $y))
    (O
        (pure
            (Lt_f64 $x $r)
            $r
            (lt_f64 (f64_from_string $y) (f64_from_string 27.5))
        )
    )
)

(exec 1
    (, (Age $x $y))
    (O
        (pure
            (Gt_f64 $x $r)
            $r
            (gt_f64 (f64_from_string $y) (f64_from_string 27.5))
        )
    )
)

(exec 2
    (, (Age $x $y))
    (O
        (pure
            (Eq_f64 $x $r)
            $r
            (eq_f64 (f64_from_string $y) (f64_from_string 27.0))
        )
    )
)

(exec 3
    (, (Age $x $y))
    (O
        (pure
            (Ne_f64 $x $r)
            $r
            (ne_f64 (f64_from_string $y) (f64_from_string 27.0))
        )
    )
)

(exec 4
    (, (Age $x $y))
    (O
        (pure
            (Le_f64 $x $r)
            $r
            (le_f64 (f64_from_string $y) (f64_from_string 27.5))
        )
    )
)

(exec 5
    (, (Age $x $y))
    (O
        (pure
            (Ge_f64 $x $r)
            $r
            (ge_f64 (f64_from_string $y) (f64_from_string 27.5))
        )
    )
)
